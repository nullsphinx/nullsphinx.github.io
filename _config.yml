# ----------------  _config.yml  ----------------
# Pick *one* theme method.  We’ll use remote_theme.
remote_theme: just-the-docs/just-the-docs   # sidebar + search

title: nullsphinx
search_enabled: true

just_the_docs:
  color_scheme: dark        # 'light' or 'auto' also work
  logo: /assets/logo.svg    # optional
